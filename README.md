# breast-cancer-classification-ml
本项目基于 scikit-learn 对威斯康星乳腺癌数据集进行良恶性分类，比较了 KNN（k=5）、SVM（RBF核）、决策树（max_depth=5）和随机森林（n_estimators=100）四种算法的性能。首先加载数据、进行标准化和分层抽样划分（训练:测试=8:2），并定义了统一的评估函数，输出准确率、精确率、召回率、F1分数、AUC及混淆矩阵。项目验证了机器学习在乳腺癌自动诊断中的可行性，SVM和随机森林可作为优先选型方案，同时指出可通过超参数调优、特征选择或模型融合进一步提升性能。
